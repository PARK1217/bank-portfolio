-- ===================================================================
-- 02_seed.sql — 시드 데이터 (placeholder)
-- ===================================================================
-- 01_schema.sql 은 라이브 Docker DB(bank-portfolio-postgres)에서
-- pg_dump --schema-only 로 추출한 v52 영문 스키마입니다.
--
-- 시드 데이터는 기능 구현 진행에 따라 필요 시점에 이 파일에 추가합니다.
-- docker-entrypoint-initdb.d 가 01_schema.sql 다음으로 이 파일을
-- 실행하므로, 이 파일은 비어 있어도(주석만) 안전합니다.
-- ===================================================================