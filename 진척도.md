# 다온뱅크 프로젝트 진척도

> **기준**: 2026-05-22 (금) · **발표 D-Day**: 2026-06-08 (월) · **남은 일수**: **17일**
> **방금 완료**: §2 #3 frontend-admin Phase 6 **풀체인 브라우저 검증 완료** — Playwright Python + Chromium 헤드리스로 ADMIN001 로그인 → 사이드바 6그룹 메뉴 9페이지(dashboard·review-queue·decisions·repayments·customers·accounts·overdue·health·audit·observability) + 동적 상세 4건(loans/[appId]·attachments·customers/[custNo]·overdue/[custNo]) 순회. 결과: 모든 페이지 200 + 본문 정상 렌더링, API 4xx/5xx 0건, 동적 상세 4건 동작, KPI/테이블/Phoenix iframe 다 표시. 스크린샷 14장 캡처(admin_01~admin_23). 잔존 워닝: dev 모드 Next.js Link prefetch 가 `/health` RSC 응답을 abort 로 잡는 "Failed to fetch RSC payload" 콘솔 1줄(브라우저가 자동 fallback navigation → 사용자 영향 없음, prod 빌드에서 사라짐). 직전: §2 #12 비번찾기 본인인증 단순화.

---

## ⭐ 한 줄 요약

> **MVP/Signature 화면·백엔드는 거의 다 됐다. 남은 건 Phoenix 임베드 + 인계 노트 픽스 + frontend-admin 부트스트랩 + 발표 자료.**

---

## 1. 한눈에 — Phase 별 일정 vs 실제

```
       0     20     40     60     80    100 %
Phase0 ██████████████████████████████████████ 100  ✅  (예정 5/16~5/18 · 종료)
Phase1 ███████████████████████████████████░░░  95  🚀  (예정 5/18~5/27 · 일정 30%)
Phase2 ██████████████████████████████████░░░░  90  🚀  (예정 5/18~5/29 · 일정 27%)
Phase3 █████████████████████████████████░░░░░  85  🚀  (예정 5/18~5/28 · 일정 27%)
Phase4 ████████████░░░░░░░░░░░░░░░░░░░░░░░░░░  30  🚀  (예정 5/29~6/5  · 일정  0%)
Phase5 ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░   0  —   (예정 6/6 ~6/8  · 일정  0%)
Phase6 ██████████████████████████████████████ 100  ✅  (예정 6/1 ~6/7  · 일정  0%) ← 백엔드 100%, 프론트 100% (브라우저 풀체인 검증만 남음)
```

**계획 대비 약 7~10일 앞서감.**

---

## 2. 🔴 끊어진 연결·진입점 (지금 화면/흐름이 막혀 있는 곳)

| # | 끊긴 곳 | 결과 (사용자가 막히는 지점) | 픽스 위치 / 상태 |
| --- | --- | --- | --- |
| 1 | ~~**FOREIGN 상품 가입 CTA 부재**~~ → **✅** | `openCtaTarget` + `nextOpenUrl` 모두 FOREIGN 케이스 이미 추가됨. 이번 세션에서 `TYPE_LABEL["FOREIGN"]="외화"` 보강 (상품 상세 헤더 라벨). `/products/106` "가입하기 →" 노출 + terms → /open-foreign 폼 정상 라우팅 | (완료) |
| 2 | ~~**JOINT/MINOR 폼 약관 가드 부재**~~ → **✅** | `open-joint/page.tsx` · `open-minor/page.tsx` 둘 다 `useEffect getProductOpenSession()` 가드 추가 (open-saving/deposit/foreign 과 동일 패턴, 미동의 시 `/terms?as=joint`/`?as=minor` 로 replace) | (완료) |
| 3 | ~~**frontend-admin/ 디렉토리 자체 없음**~~ → **✅ 100% (브라우저 e2e 검증 완료)** | 실제 화면 19개 가동 중 (host 5001 npm dev). 백엔드 라우트도 모두 200. Playwright Chromium 헤드리스 풀체인: ADMIN001 로그인 → 사이드바 6그룹 9페이지 + 동적 상세 4건(loans/[appId]·attachments·customers/[custNo]·overdue/[custNo]) 모두 200·본문 정상·API 4xx/5xx 0건. KPI/테이블/외부망 카드 5종(UP)·HUMAN_REVIEW 큐 김미선 20004·Phoenix iframe 모두 표시. 잔존: dev-only Next.js Link prefetch RSC 워닝 1줄(사용자 영향 없음, prod 사라짐) | (완료) |
| 4 | ~~Phoenix 임베드 미착수~~ → **1단계 ✅** | Phoenix UI 6006 에서 backend trace 자동 표시 중 (P50 53ms / P99 5.5s). 다음 단계: admin 화면 iframe 임베드 + Faithfulness 자동 평가 — #3 frontend-admin 의존 | (1단계 완료, 2단계는 #3 의존) |
| 5 | ~~**AI_RAG_EVALUATION 컨슈머 발행측 미통합**~~ → **✅** | `service/chatbot.py:357` 이미 `TOPIC_CHATBOT_RAG_EVALS` 발행 통합. `main.py:86` 컨슈머 등록. DB 적재 e2e 확인: AI_LLM_CALL_LOG 7건 + AI_RAG_EVALUATION 2건 (실측 2026-05-22) | (완료) |
| 6 | **외부 통신 모니터링 — 백엔드 ✅ / 화면 ❌** | `service/admin_health.py` 워커가 KFTC/BOK_WIRE/MY_DATA/NICE/KCB 5종 mock ping 적재 중 (실측 EXTERNAL_API_HEALTH 최신 10건 모두 UP/DEGRADED 정상). 화면은 #3 frontend-admin 의존 | (백엔드 완료, 화면은 #3 의존) |
| 7 | ~~시드 999999 `PARTY_ID=NULL`~~ → **✅** | `docker-compose.yml:19-23` 에 `12_seed_999999_party.sql` 마운트 이미 추가됨 — fresh init 시 자동 적용 (PARTY 1999 홍길동 + INDIVIDUAL_PARTY + CUSTOMER UPDATE) | (완료) |
| 8 | ~~**FOREIGN PRODUCT_TERMS_MAPPING 시드 누락**~~ → **✅** | DB 실측: PRODUCT_TERMS_MAPPING 에 106 매핑 4건(TERMS_ID 5/6/7/1104) + 205 매핑 4건(3/6/7/1205) 적재. `/api/products/106.terms_mappings.length=4` 정상 응답 | (완료) |
| 9 | ~~**INSTALL 가입 완료 product_name 폴백**~~ → **✅** | `service/account_product_map.py` 공유 모듈 신설 완료. `service/account_open.py:125` (INSTALL) + `api/product_open.py:264` (SAVING/DEPOSIT/FOREIGN/JOINT/MINOR) 모두 `set_product()` 호출, `complete/{token}` 응답이 `get_product()` 로 진짜 상품명 조회 | (완료) |
| 10 | ~~**MINOR 시나리오 ↔ 코드 불일치**~~ → **✅** | `_fetch_and_validate_product` 에 `expected_type: str \| tuple[str, ...]` 확장 + `open_minor` 가 SAVING/INSTALL 둘 다 허용. INSTALL 인 경우 ACCOUNT_TYPE_CD='INSTALL' + alias='어린이 적금'. e2e: 304→200(INSTALL/어린이 적금), 101→200(SAVING/어린이 통장), 201→422 거부 | (완료) — 적금 자동이체 자동 등록은 후속 작업 |
| 11 | ~~**대출 상품 카드 → precheck 화면 미동작**~~ → **✅** | 원인: 프론트가 `/api/loans/{product_id}/precheck` 호출인데 백엔드는 `/api/loans/precheck` 만 존재 → 404. `api/loan.py` 에 `@router.post("/{product_id}/precheck")` 별칭 라우트 추가 (precheck_dsr 는 product_id 미사용, 향후 product-specific 확장 자리). e2e: 401·precheck 200 OK | (완료) |
| 12 | ~~**비번찾기 본인인증 UX·정합 결함 3건**~~ → **✅** | (a) 주민번호 앞 6자리 입력 강요 — 한국 은행 표준은 본인인증사(NICE/PASS) 위임이라 과한 입력 → `resident_no` 필드 제거. (b) `password.py:138` 이 `CONTACT_TYPE_CD='PHONE'` 만 검색하는데 시드는 `MOBILE` 로 적재 → 시드 사용자 모두 휴대폰 검증 NO-OP. `IN ('MOBILE','PHONE')` 으로 통합. (c) mock SMS 라 인증번호 확인 불가 + "OTP" 명칭이 인증앱 TOTP 와 혼동 → `dev_auth_code` 응답 노출 + 화면 강조 박스/toast + 용어 "인증번호" 로 통일. 시드 999999 `CUSTOMER_CONTACT` MOBILE / 01011111111 보강. e2e: init→verify→reset→login 200·휴대폰 불일치 422·시드 100001 MOBILE 200 | (완료) |

전체 인계 노트는 `WORKBOARD.md ## 유의점` 13건 참조 (이번 세션이 추가: list endpoint 응답 형식 비일관, 대출 precheck 화면 미동작).

---

## 3. 🎯 앞으로 할 일 — 우선순위·의존성·일정

### 🔥 즉시 (오늘~내일, 5/22~5/23)

| 작업 | 의존성 | 예상 비용 | 효과 |
| --- | --- | --- | --- |
| ~~**Phoenix 임베드 1단계**~~ → ✅ 완료 | — | — | Phoenix UI 6006 자동 trace 적재 중 |
| ~~**인계 노트 #1·#2 픽스 (FOREIGN CTA + JOINT/MINOR 가드)**~~ → ✅ 완료 | — | — | 화면 흐름 정합 |
| ~~**인계 노트 #10 MINOR 시나리오 결정**~~ → ✅ 완료 (open_minor INSTALL 확장, 304→200) | — | — | 시나리오↔코드 정합 |
| **`frontend-admin/` Next.js 부트스트랩 (#3)** | 없음 | 반나절 | 관리자 화면 시연 + Phoenix iframe + 외부 헬스 카드 한 번에 unblock |

### 📋 단기 (5/23~5/28, Phase 4 통합 QA 시작 전)

| 작업 | 의존성 | 예상 비용 |
| --- | --- | --- |
| ~~**#5 AI_RAG_EVALUATION 발행 통합**~~ → ✅ 완료 (DB e2e LLM_CALL 7 + RAG_EVAL 2) | — | — |
| ~~**#7·#8 시드 999999 PARTY_ID + FOREIGN TERMS_MAPPING**~~ → ✅ 완료 (compose 마운트 + 106·205 4건씩 시드) | — | — |
| ~~**#9 INSTALL product_name 폴백 픽스**~~ → ✅ 완료 (account_product_map.py 공유 모듈) | — | — |
| ~~**#6 EXTERNAL_API_HEALTH 백엔드**~~ → ✅ 완료 (admin_health 워커 5종 적재) | — | — |
| ~~**#11 LOAN precheck 별칭 라우트**~~ → ✅ 완료 (`/api/loans/{product_id}/precheck` 200) | — | — |
| **시드 9개 시나리오 수동 풀체인 검증** (Phase 4 선행) | 검증 자격증명 정리 후 | 하루 |
| **WORKBOARD 챗봇 path 미스매치 4건 (sessions/terms-search/source/handoff)** | 결정 필요 | 1~2시간 |

### 🏗️ Phase 6 화면 (5/29~6/7, D-8 ~ D-17) — **대부분 완료**

| 작업 | 의존성 | 예상 비용 |
| --- | --- | --- |
| ~~**`frontend-admin/` Next.js 부트스트랩**~~ → ✅ 완료 (이미 host 5001 가동) | — | — |
| ~~**관리자 로그인 화면 + 대시보드 셸**~~ → ✅ 완료 | — | — |
| ~~**대출 검토 큐 화면 (§9.2.4)**~~ → ✅ 완료 (review-queue + decisions + [appId] + attachments) | — | — |
| ~~**회원 연체 추적 화면 (§9.2.5)**~~ → ✅ 완료 (overdue + [custNo] + Risk Badge) | — | — |
| ~~**Phoenix iframe 임베드 화면**~~ → ✅ 완료 (`/observability` iframe + fallback) | — | — |
| ~~**외부망 헬스 카드 (§9.2.3)**~~ → ✅ 완료 (백엔드 `/external` 200 + 시계열 + 5종 적재) | — | — |
| ~~**관리자 감사 로그 뷰**~~ → ✅ 완료 (`/audit` 필터 6종 + facets) | — | — |

### 📺 마감 (6/6~6/8, D-16~D-18)

| 작업 | 의존성 | 예상 비용 |
| --- | --- | --- |
| **발표 자료 (아키텍처/ERD/시퀀스 다이어그램)** | 코드 프리징 후 | 하루 |
| **데모 영상** | 화면 모두 안정화 | 반나절 |
| **시드 9개 최종 회귀 검증** | — | 반나절 |

---

## 4. ✅ 무엇까지 진행했는지 — Phase 상세

### Phase 0 — 셋업 ✅ 100%
- Docker Compose 4 서비스 가동 (postgres / backend / frontend / kafka)
- FastAPI + Next.js + asyncpg + aiokafka 보일러플레이트 완료
- `GET /health` 핑 정상

### Phase 1 — 백엔드 코어 ✅ 95%

| § | 영역 | 상태 | 산출물 |
| --- | --- | --- | --- |
| 1 | DB 스키마 | ✅ | `db/01_schema.sql` + `03_v53_migration.sql` + `04~11` (11개 마이그레이션) |
| 2 | 시드 | ✅ | `02_seed.sql` (25 상품) + `05_persona_seed.sql` (5명) + `06`/`09` |
| 3 | 인증 (AU-001~012) | ✅ | `auth` / `signup` / `password` / `setup-OTP/SIMPLE-PIN/biometric` |
| 4 | 계좌 (AC-001/002/004/005) | ✅ | `account.py` + 화면 8개 |
| 5 | 상품 (OP-001~010) | ✅ | `product` / `product_open` (5종 open-* + terms + complete) |
| 6 | 이체 3-tier (TR-001~003) | ✅ | `transfer.py` INTRA/KFTC/BOK-Wire+ + Kafka 정산 컨슈머 |
| 7 | 자동이체 (TR-005~008) | ✅ | `auto_transfer` + `auto_transfer_worker`(실행 트리거 완료) + 1회 예약(scheduled, ONCE) + 빠른선택 UI + verify 선행 강제 |
| 8 | 대출 (LN-001~009) | ✅ | `loan.py` + 9 화면 풀체인 |
| 9 | 글로벌 공통 | ✅ | middleware/예외/로깅/PII 마스킹/request_id |

### Phase 2 — 프론트 MVP ✅ 90%

**총 80+ 화면**:
- accounts 8 · loans 9 · transfer 8 · products 11 · security 8 · signup 6 · chatbot 5 · asset-analysis 4 · 기타 15+
- 발표 체크리스트 데모 9종 화면 모두 존재

### Phase 3 — RAG ✅ 85%
- 챗봇 3-tier (FAQ/KEYWORD/VECTOR) + 환각방지 게이팅
- 자산분석 RAG 풀체인 (survey → processing → result → product)
- `AI_LLM_CALL_LOG` Kafka 컨슈머 영구화 완료
- `rag_eval_log.py` 핸들러 준비 (발행측 통합 대기) ← **#5 끊긴 연결**
- ⚠️ Phoenix 통합 0건 ← **#4 끊긴 연결**

### Phase 4 — 통합/QA 🚀 30% (선행)
- 대출 LN-001~009 풀체인 ✅
- 이체 INTRA/KFTC 풀체인 (멱등성+동시성+역분개) ✅
- 자동이체 워커 e2e ✅
- 상품 개설 5종 브라우저 풀체인 자동화 ✅ (Playwright Chromium)
- FDS 의심거래 e2e ✅
- 관리자 인증·감사 e2e ✅

### Phase 5 — 마감 ⏳ 0%

### Phase 6 — 관리자 페이지 ✅ 100% (백엔드 100% / 프론트 100%)

frontend-admin 디렉토리는 **이미 존재**하며 host 5001 (`npm run dev -p 5001`) 가동 중. 화면 19개·핵심 라우트 e2e ADMIN001 로그인 검증 완료. 시드 자격증명: `ADMIN001 / admin1234` (여신정책팀), `AUDIT001 / admin1234` (감사팀). 남은 건 브라우저 풀체인 클릭 검증.

| 영역 | § | 백엔드 | 프론트 |
| --- | --- | --- | --- |
| 인증·권한 | 9.2.1 | ✅ admin_auth + ADMIN_SESSION + ME/logout | ✅ `/login`·AdminAuthProvider·자동 로그아웃 |
| Phoenix 임베드 | 9.2.2 | ✅ Phoenix 6006 가동·trace 적재 | ✅ `/observability` iframe + 차단 시 fallback |
| 외부망 모니터링 | 9.2.3 | ✅ `/external` 5종 200 (KFTC DOWN·BOK_WIRE/MY_DATA/NICE UP·KCB UP) + `/external/{api_name}` 시계열 | ✅ `/health` KPI 4 + API 카드 |
| 대출 검토·첨부 | 9.2.4 | ✅ admin_loan + attachments 일치성 | ✅ `/loans/review-queue`·`/decisions`·`/[appId]`·`/attachments` |
| 연체 추적 | 9.2.5 | ✅ admin_overdue + 회원 상세 | ✅ `/overdue`·`/overdue/[custNo]` + Risk Badge |
| ML 자동 승인 | 9.2.6 | ✅ loan_decision + XGBoost + HUMAN_REVIEW 라벨링 | ✅ `/loans/[appId]` 점수 시각화 + 승인·반려 |
| 감사 로그 | 9.2.7 | ✅ admin_audit + 미들웨어 + `GET /api/admin/audit/logs` (필터·페이지네이션 200) | ✅ `/audit` 필터 6종 + facets |
| 회원·계좌 조회 | (추가) | ✅ admin_customer | ✅ `/customers`·`/customers/[custNo]`·`/accounts`·`/accounts/[accountNo]` |
| 대출 상환 | (추가) | ✅ admin_loan_repay | ✅ `/loans/repayments`·`/loans/repayments/[loanContractNo]` |

---

## 5. 환경·자격증명 (cold start 용)

### Docker
- `postgres` 컨테이너 (host 5434 → 컨테이너 5432, DB 이름 `bank`, 사용자 `bank`/`bank1234`)
- `backend` (host 8001 → 컨테이너 8000) — CORS_ORIGINS 에 5001 등록됨
- `kafka` (9092) · `phoenix` (6006)
- `frontend` Docker 컨테이너 (host 3001 → 컨테이너 3000)
- `frontend-admin` host `npm run dev -p 5001` (docker-compose 미등록 — Phase 6 운영 환경 시 추가 검토)

### 자격증명
| 페르소나 | 이메일 | 비번 | CUSTOMER_NO | PARTY_ID | 비고 |
| --- | --- | --- | --- | --- | --- |
| 박철수 | `park@daon.example` | `demo1234` | 100001 | 1001 | ✅ JOINT/MINOR 영구화 검증 가능 |
| 김영희 | `kim.yh@daon.example` | `demo1234` | 100002 | 1002 | 공동명의자로 사용 |
| 최지영 (자녀) | — | — | 100003 | 1003 | MINOR 가입 시 child_party_id |
| 김연체 | `kim.yc@daon.example` | `demo1234` | 100004 | 1004 | 연체 시나리오 |
| 김미선 | `kim.ms@daon.example` | `demo1234` | 100005 | 1005 | 마통 시나리오 |
| 시드 999999 | `test@example.com` | `testpass123!` | 999999 | 1999 (홍길동) | ✅ db/12 fix (PARTY 1999 + CUSTOMER_CONTACT MOBILE 01011111111) — JOINT/MINOR 가드·비번찾기 본인인증 모두 통과 |

### 도구
- Playwright Python 1.60 + Chromium 1223 — 브라우저 e2e 자동화 가능
- `pyotp` (실시간 TOTP)
- bcrypt 12 rounds (비번 해시)

---

## 6. 다음 세션 cold start 체크리스트

1. `git status` — 다른 세션 변경사항 확인
2. `WORKBOARD.md` — `## 진행 중` + `## 유의점` 읽기
3. `진척도.md` (이 파일) — Phase 별 상태 + 끊긴 연결 확인
4. `docker compose ps` — 4 서비스 다 떠 있는지 확인 (`postgres`/`backend`/`kafka` + host `:3001` 프론트)
5. 이전 세션 메모리: `session-recap-2026-05-21.md` + `session-snapshot-2026-05-21-evening.md`